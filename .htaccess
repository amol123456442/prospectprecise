<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /prospectprecise/

    # agar request physical file/folder nahi hai to index.php me bhejo
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
